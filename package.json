{
  "name": "portfolioweb",
  "version": "1.0.0",
  "description": "Zach Martim's portfolio. The site is in portfolio-frontend/, its chat backend in functions/.",
  "private": true,
  "scripts": {
    "build": "npm --prefix portfolio-frontend run build",
    "start": "npm --prefix portfolio-frontend start",
    "deploy": "npm run build && firebase deploy"
  },
  "license": "MIT"
}
